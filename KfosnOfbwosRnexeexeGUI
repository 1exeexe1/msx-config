-- Custom ¥harrz UI - Enhanced with OP Tab functionality
-- LocalScript (via loader/exploit)

local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")
local StarterGui = game:GetService("StarterGui")
local ReplicatedStorage = game:GetService("ReplicatedStorage")

local player = Players.LocalPlayer
local playerGui = player:WaitForChild("PlayerGui")
local character = player.Character or player.CharacterAdded:Wait()
local brick = ReplicatedStorage:WaitForChild("Brick", 5) -- Wait a bit for it
local paintTool = player.Backpack:FindFirstChild("Paint") or character:FindFirstChild("Paint")
local remote = paintTool and (paintTool:FindFirstChild("Event") or paintTool:FindFirstChildWhichIsA("RemoteEvent"))

-- Early exit if critical missing
if not remote or not brick then
    warn("Missing Paint tool/remote or Brick in ReplicatedStorage!")
end

local screenGui = Instance.new("ScreenGui")
screenGui.Name = "YharrzUI"
screenGui.ResetOnSpawn = false
screenGui.Parent = playerGui

-- [Previous Bubble + MainFrame + TopBar code remains the same - omitted for brevity, assume it's here]

-- Content Frames for tabs
local homeContent = Instance.new("Frame")
homeContent.Name = "HomeContent"
homeContent.Size = UDim2.new(1,0,1,0)
homeContent.BackgroundTransparency = 1
homeContent.Visible = true
homeContent.Parent = contentFrame  -- from previous code

local opContent = Instance.new("Frame")
opContent.Name = "OPContent"
opContent.Size = UDim2.new(1,0,1,0)
opContent.BackgroundTransparency = 1
opContent.Visible = false
opContent.Parent = contentFrame

-- Simple tab switch (expand later)
homeTabBtn.MouseButton1Click:Connect(function()
    homeContent.Visible = true
    opContent.Visible = false
end)

opTabBtn.MouseButton1Click:Connect(function()
    homeContent.Visible = false
    opContent.Visible = true
end)

-- =====================================
-- OP Tab - Custom Controls
-- =====================================

local goldenColor = Color3.fromRGB(255, 215, 0)
local sides = {"Upside", "Leftside", "Frontside", "Rightside", "Backside", "Downside"}
local sideNormals = {
    Upside   = Enum.NormalId.Top,
    Leftside = Enum.NormalId.Left,
    Frontside = Enum.NormalId.Front,
    Rightside = Enum.NormalId.Right,
    Backside  = Enum.NormalId.Back,
    Downside  = Enum.NormalId.Bottom
}

local texts = {}    -- side -> text
local colors = {}   -- side -> Color3

local rootPos = character:WaitForChild("HumanoidRootPart").Position
local key = "both \u{1F91D}"
local PURE_BLACK = Color3.fromRGB(0,0,0)

-- Discord text (bottom, customizable)
local discordText = 'D<font size= "0">ddxdsxc</font>is<font size= "0">ddxdsxc</font>c<font size= "0">ddxdsxc</font>o<font size= "0">ddxdsxc</font>rd.<font size= "0">ddxdsxc</font>gg/<font size= "0">ddxdsxc</font>d7qJCEHE8t'

-- Block color
local blockColor = PURE_BLACK

-- Create 6 side rows + Discord row
for i, sideName in ipairs(sides) do
    local row = Instance.new("Frame")
    row.Size = UDim2.new(1, 0, 0, 50)
    row.Position = UDim2.new(0, 0, 0, (i-1)*55)
    row.BackgroundTransparency = 1
    row.Parent = opContent

    local label = Instance.new("TextLabel")
    label.Size = UDim2.new(0.2, 0, 1, 0)
    label.Text = sideName .. ":"
    label.TextColor3 = Color3.new(1,1,1)
    label.BackgroundTransparency = 1
    label.Font = Enum.Font.GothamSemibold
    label.TextXAlignment = Enum.TextXAlignment.Left
    label.Parent = row

    local textBox = Instance.new("TextBox")
    textBox.Size = UDim2.new(0.5, 0, 0.8, 0)
    textBox.Position = UDim2.new(0.22, 0, 0.1, 0)
    textBox.Text = "Enter Text Here"
    textBox.ClearTextOnFocus = false
    textBox.BackgroundColor3 = Color3.fromRGB(40,40,50)
    textBox.TextColor3 = Color3.new(1,1,1)
    textBox.Font = Enum.Font.Gotham
    textBox.Parent = row

    local colorPreview = Instance.new("Frame")
    colorPreview.Size = UDim2.new(0.15, 0, 0.8, 0)
    colorPreview.Position = UDim2.new(0.8, 0, 0.1, 0)
    colorPreview.BackgroundColor3 = Color3.fromRGB(150 + i*15, 100, 255 - i*30) -- initial varied
    colorPreview.BorderSizePixel = 0
    colorPreview.Parent = row

    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0,6)
    corner.Parent = colorPreview

    -- Simple color input (for now TextBox RGB, e.g. "255,100,50")
    local colorInput = Instance.new("TextBox")
    colorInput.Size = UDim2.new(0.13, 0, 0.6, 0)
    colorInput.Position = UDim2.new(0.85, 0, 0.2, 0)
    colorInput.Text = "R,G,B"
    colorInput.BackgroundTransparency = 0.6
    colorInput.TextScaled = true
    colorInput.Parent = row

    colorInput.FocusLost:Connect(function(enter)
        if enter then
            local parts = string.split(colorInput.Text, ",")
            if #parts == 3 then
                local r,g,b = tonumber(parts[1]), tonumber(parts[2]), tonumber(parts[3])
                if r and g and b then
                    local col = Color3.fromRGB(math.clamp(r,0,255), math.clamp(g,0,255), math.clamp(b,0,255))
                    colorPreview.BackgroundColor3 = col
                    colors[sideName] = col
                end
            end
        end
    end)

    textBox.FocusLost:Connect(function(enter)
        if enter then
            texts[sideName] = textBox.Text
        end
    end)
end

-- Discord text row (7th)
local discordRow = Instance.new("Frame")
discordRow.Size = UDim2.new(1,0,0,50)
discordRow.Position = UDim2.new(0,0,0,6*55)
discordRow.BackgroundTransparency = 1
discordRow.Parent = opContent

local dLabel = Instance.new("TextLabel")
dLabel.Size = UDim2.new(0.3,0,1,0)
dLabel.Text = "Bottom Discord Text:"
dLabel.TextColor3 = Color3.new(1,1,1)
dLabel.BackgroundTransparency = 1
dLabel.Parent = discordRow

local dTextBox = Instance.new("TextBox")
dTextBox.Size = UDim2.new(0.7,0,0.8,0)
dTextBox.Position = UDim2.new(0.3,0,0.1,0)
dTextBox.Text = discordText
dTextBox.ClearTextOnFocus = false
dTextBox.BackgroundColor3 = Color3.fromRGB(40,40,50)
dTextBox.TextColor3 = Color3.new(1,1,1)
dTextBox.Parent = discordRow

dTextBox.FocusLost:Connect(function()
    discordText = dTextBox.Text
end)

-- Block color picker row
local blockRow = Instance.new("Frame")
blockRow.Size = UDim2.new(1,0,0,50)
blockRow.Position = UDim2.new(0,0,0,7*55)
blockRow.BackgroundTransparency = 1
blockRow.Parent = opContent

local blockLabel = Instance.new("TextLabel")
blockLabel.Size = UDim2.new(0.3,0,1,0)
blockLabel.Text = "Block Color:"
blockLabel.TextColor3 = Color3.new(1,1,1)
blockLabel.BackgroundTransparency = 1
blockLabel.Parent = blockRow

local blockPreview = Instance.new("Frame")
blockPreview.Size = UDim2.new(0.15,0,0.8,0)
blockPreview.Position = UDim2.new(0.35,0,0.1,0)
blockPreview.BackgroundColor3 = blockColor
blockPreview.Parent = blockRow

local blockInput = Instance.new("TextBox")
blockInput.Size = UDim2.new(0.2,0,0.6,0)
blockInput.Position = UDim2.new(0.55,0,0.2,0)
blockInput.Text = "R,G,B"
blockInput.Parent = blockRow

blockInput.FocusLost:Connect(function(enter)
    if enter then
        local p = string.split(blockInput.Text, ",")
        if #p == 3 then
            local r,g,b = tonumber(p[1]),tonumber(p[2]),tonumber(p[3])
            if r and g and b then
                blockColor = Color3.fromRGB(math.clamp(r,0,255),math.clamp(g,0,255),math.clamp(b,0,255))
                blockPreview.BackgroundColor3 = blockColor
            end
        end
    end
end)

-- Action Buttons (golden)
local buttonY = 8.2*55
local function createGoldenButton(name, posX, callback)
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(0.48, 0, 0, 50)
    btn.Position = UDim2.new(posX, 10, 0, buttonY)
    btn.BackgroundColor3 = goldenColor
    btn.TextColor3 = Color3.new(0,0,0)
    btn.Text = name
    btn.Font = Enum.Font.GothamBold
    btn.TextScaled = true
    btn.Parent = opContent

    local stroke = Instance.new("UIStroke")
    stroke.Color = Color3.fromRGB(255, 235
