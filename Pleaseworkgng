local Rayfield = loadstring(game:HttpGet("https://sirius.menu/rayfield"))()
local Teleport = loadstring(game:HttpGet("https://pastebin.com/raw/90Ah2xj8"))()
task.wait(0.5)
local Notify = Rayfield:Notify({
    Content = ((("UPDATED VERSION: " .. Teleport) .. ", YOUR VERSION: ") .. "1.0.2"),
    Duration = 6.5,
    Image = 4483362458,
    Title = "OUT OF DATE!!"
})
local RunService = game:GetService("RunService")
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local LocalPlayer2 = Players.LocalPlayer
local mouse = LocalPlayer2:GetMouse()
local Paint = object:FindFirstChild("Paint")
local Script = Paint:FindFirstChild("Script")
local Event = Script:FindFirstChild("Event")
local Window = Rayfield:CreateWindow({
    LoadingTitle = "COOLER THAN EVER!! (report bugs to @controller5256 on discord)",
    Discord = {
        Invite = "5bjPEgka",
        Enabled = true,
        RememberJoins = true
    },
    LoadingSubtitle = "by XxHunterStormBytexX4",
    Name = "Hunterstorm's unlisted trolling ui",
    KeySettings = {
        Key = {"https://pastebin.com/raw/3crV1nc4"},
        Subtitle = "key",
        Title = "key system",
        SaveKey = true,
        GrabKeyFromSite = true,
        Note = "join our discord server ((discord.gg/5bjPEgka))",
        FileName = "Boona20562"
    },
    ConfigurationSaving = {
        FileName = "Big Hub",
        Enabled = false
    },
    ToggleUIKeybind = "K",
    KeySystem = true
})
local Tab = Window:CreateTab("main", "terminal")
local Button = Tab:CreateButton({
    Callback = function(bI)
        local InfiniteYield = loadstring(game:HttpGet("https://raw.githubusercontent.com/EdgeIY/infiniteyield/master/source"))()
    end,
    Name = "infinite yield"
})
local Button2 = Tab:CreateButton({
    Callback = function(bI)
        local FireServer = game.ReplicatedStorage.System:FireServer("xl")
    end,
    Name = "join xl (in case u cant join because roblox thinks ur spamming)"
})
local Dropdown = Tab:CreateDropdown({
    Flag = "MaterialDropdown",
    Name = "rainbow material",
    CurrentOption = {"neon"},
    Options = {
    "smooth",
    "plastic",
    "tiles",
    "bricks",
    "planks",
    "ice",
    "neon",
    "toxic",
    "anchor"
},
    Callback = function(selected) end,
    MultipleOptions = false
})
local Toggle = Tab:CreateToggle({
    CurrentValue = false,
    Flag = "RainbowPaint",
    Callback = function(enabled) end,
    Name = "rainbow paint"
})
local Toggle2 = Tab:CreateToggle({
    CurrentValue = false,
    Flag = "shaval",
    Callback = function(enabled) end,
    Name = "shovel"
})
local Toggle3 = Tab:CreateToggle({
    CurrentValue = false,
    Flag = "deeleeete",
    Callback = function(enabled) end,
    Name = "delete"
})
local Toggle4 = Tab:CreateToggle({
    CurrentValue = false,
    Flag = "RainbowSky",
    Callback = function(enabled) end,
    Name = "rainbow sky"
})
local Button3 = Tab:CreateButton({
    Callback = function() end,
    Name = "Select Block"
})
local conn = mouse.Button1Down:Connect(function(...)
    local Notify2 = Rayfield:Notify({
        Content = "Selected block: Instance",
        Duration = 6.5,
        Image = 4483362458,
        Title = "block selected"
    })
end)
local Dropdown2 = Tab:CreateDropdown({
    Flag = "Dropdown1",
    Name = "block path type",
    CurrentOption = {"detailed"},
    Options = {"normal", "detailed", "rainbow detailed", "rainbow normal"},
    Callback = function(selected) end,
    MultipleOptions = false
})
local Toggle5 = Tab:CreateToggle({
    CurrentValue = false,
    Flag = "dbp",
    Callback = function(enabled) end,
    Name = "blocks path"
})
local Input = Tab:CreateInput({
    Flag = "Input1",
    Name = "spray paint",
    CurrentValue = "",
    PlaceholderText = "GET NOOBED",
    Callback = function(text) end,
    RemoveTextAfterFocusLost = false
})
local Toggle6 = Tab:CreateToggle({
    CurrentValue = false,
    Flag = "dbp",
    Callback = function(enabled) end,
    Name = "spray paint"
})
local Toggle7 = Tab:CreateToggle({
    CurrentValue = false,
    Flag = "dbp",
    Callback = function(bI)
        print(true)
        print("2: true")
    end,
    Name = "kill aura"
})
local Toggle8 = Tab:CreateToggle({
    CurrentValue = false,
    Flag = "dca",
    Callback = function(bI)
        print(true)
        print("2: true")
    end,
    Name = "Delete aura (CLICK AT POSITION)"
})
local Backpack = LocalPlayer2:WaitForChild("Backpack")
local Delete = Backpack:FindFirstChild("Delete")
local Script2 = Delete:FindFirstChild("Script")
local FindFirstChild = Delete.Script:FindFirstChild("Event")
local Shovel = Backpack:FindFirstChild("Shovel")
local Script3 = Shovel:FindFirstChild("Script")
local FindFirstChild2 = Shovel.Script:FindFirstChild("Event")
local Button4 = Tab:CreateButton({
    Callback = function(bI)
        local Paint2 = object:FindFirstChild("Paint")
        local Script4 = Paint2:FindFirstChild("Script")
        local Event2 = Script4:FindFirstChild("Event")
    end,
    Name = "reset paint variable (ONLY FOR PAINT, NO OTHER TOOL!!)"
})
local Button5 = Tab:CreateButton({
    Callback = function(bI)
        local Shovel2 = object:FindFirstChild("Shovel")
        local Script5 = Shovel2:FindFirstChild("Script")
        local FindFirstChild3 = Shovel2.Script:FindFirstChild("Event")
        local Delete2 = object:FindFirstChild("Delete")
        local Script6 = Delete2:FindFirstChild("Script")
        local FindFirstChild4 = Delete2.Script:FindFirstChild("Event")
    end,
    Name = "reset tool variables"
})
local Tab2 = Window:CreateTab("OP", "terminal")
local Input2 = Tab2:CreateInput({
    Flag = "Input1",
    Name = "repStorage's brick text",
    CurrentValue = "",
    PlaceholderText = "Hello, world!",
    Callback = function(text) end,
    RemoveTextAfterFocusLost = false
})
local Button6 = Tab2:CreateButton({
    Callback = function(bI)
        local Paint3 = object:FindFirstChild("Paint")
        local Script7 = Paint3:FindFirstChild("Script")
        local Event3 = Script7:FindFirstChild("Event")
        local FindFirstChild5 = game.Players.LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
        local FindFirstChild6 = game.ReplicatedStorage:FindFirstChild("Brick")
        local Paint4 = object:FindFirstChild("Paint")
        Paint4.Parent = {}
        print("SET GEAR: Paint")
        FindFirstChild6.Position = UDim2.new(0, 0, 0, 0)
        local WaitForChild = game.ReplicatedStorage:WaitForChild("Brick")
        local WaitForChild2 = game.ReplicatedStorage:WaitForChild("Brick")
        local WaitForChild3 = game.Players.LocalPlayer.Character:WaitForChild("Paint")
        local Script8 = WaitForChild3:WaitForChild("Script")
        local Event4 = Script8:WaitForChild("Event")
        Event4:FireServer(WaitForChild, "Left", UDim2.new(0, 0, 0, 0), "both ðŸ¤", Color3.new(0.878431, 0.878431, 0.439216), "spray", "<font color=\\"#ff5555\\">N</font><font color=\\"#55ff55\\">o</font><font color=\\"#5599ff\\">Key</font>")
        local StarterGui = game:GetService("StarterGui")
        StarterGui:SetCore("SendNotification", {
    Duration = 5,
    Text = "for debugging",
    Title = "RepStorage Brick Text Set"
})
        task.wait(0.15)
        FindFirstChild6.Position = UDim2.new(0, 0, 0, 0)
        local WaitForChild4 = game.ReplicatedStorage:WaitForChild("Brick")
        local WaitForChild5 = game.ReplicatedStorage:WaitForChild("Brick")
        local WaitForChild6 = game.Players.LocalPlayer.Character:WaitForChild("Paint")
        local Script9 = WaitForChild6:WaitForChild("Script")
        local Event5 = Script9:WaitForChild("Event")
        Event5:FireServer(WaitForChild4, "Right", UDim2.new(0, 0, 0, 0), "both ðŸ¤", Color3.new(0.878431, 0.878431, 0.439216), "spray", "<font color=\\"#ff5555\\">N</font><font color=\\"#55ff55\\">o</font><font color=\\"#5599ff\\">Key</font>")
        local StarterGui2 = game:GetService("StarterGui")
        StarterGui2:SetCore("SendNotification", {
    Duration = 5,
    Text = "for debugging",
    Title = "RepStorage Brick Text Set"
})
        task.wait(0.15)
        FindFirstChild6.Position = UDim2.new(0, 0, 0, 0)
        local WaitForChild7 = game.ReplicatedStorage:WaitForChild("Brick")
        local WaitForChild8 = game.ReplicatedStorage:WaitForChild("Brick")
        local WaitForChild9 = game.Players.LocalPlayer.Character:WaitForChild("Paint")
        local Script10 = WaitForChild9:WaitForChild("Script")
        local Event6 = Script10:WaitForChild("Event")
        Event6:FireServer(WaitForChild7, "Back", UDim2.new(0, 0, 0, 0), "both ðŸ¤", Color3.new(0.878431, 0.878431, 0.439216), "spray", "<font color=\\"#ff5555\\">N</font><font color=\\"#55ff55\\">o</font><font color=\\"#5599ff\\">Key</font>")
        local StarterGui3 = game:GetService("StarterGui")
        StarterGui3:SetCore("SendNotification", {
    Duration = 5,
    Text = "for debugging",
    Title = "RepStorage Brick Text Set"
})
        task.wait(0.15)
        FindFirstChild6.Position = UDim2.new(0, 0, 0, 0)
        local WaitForChild10 = game.ReplicatedStorage:WaitForChild("Brick")
        local WaitForChild11 = game.ReplicatedStorage:WaitForChild("Brick")
        local WaitForChild12 = game.Players.LocalPlayer.Character:WaitForChild("Paint")
        local Script11 = WaitForChild12:WaitForChild("Script")
        local Event7 = Script11:WaitForChild("Event")
        Event7:FireServer(WaitForChild10, "Front", UDim2.new(0, 0, 0, 0), "both ðŸ¤", Color3.new(0.878431, 0.878431, 0.439216), "spray", "<font color=\\"#ff5555\\">N</font><font color=\\"#55ff55\\">o</font><font color=\\"#5599ff\\">Key</font>")
        local StarterGui4 = game:GetService("StarterGui")
        StarterGui4:SetCore("SendNotification", {
    Duration = 5,
    Text = "for debugging",
    Title = "RepStorage Brick Text Set"
})
        task.wait(0.15)
        FindFirstChild6.Position = UDim2.new(0, 0, 0, 0)
        local WaitForChild13 = game.ReplicatedStorage:WaitForChild("Brick")
        local WaitForChild14 = game.ReplicatedStorage:WaitForChild("Brick")
        local WaitForChild15 = game.Players.LocalPlayer.Character:WaitForChild("Paint")
        local Script12 = WaitForChild15:WaitForChild("Script")
        local Event8 = Script12:WaitForChild("Event")
        Event8:FireServer(WaitForChild13, "Top", UDim2.new(0, 0, 0, 0), "both ðŸ¤", Color3.new(0.878431, 0.878431, 0.439216), "spray", "<font color=\\"#ff5555\\">N</font><font color=\\"#55ff55\\">o</font><font color=\\"#5599ff\\">Key</font>")
        local StarterGui5 = game:GetService("StarterGui")
        StarterGui5:SetCore("SendNotification", {
    Duration = 5,
    Text = "for debugging",
    Title = "RepStorage Brick Text Set"
})
        task.wait(0.15)
        FindFirstChild6.Position = UDim2.new(0, 0, 0, 0)
        local WaitForChild16 = game.ReplicatedStorage:WaitForChild("Brick")
        local WaitForChild17 = game.ReplicatedStorage:WaitForChild("Brick")
        local WaitForChild18 = game.Players.LocalPlayer.Character:WaitForChild("Paint")
        local Script13 = WaitForChild18:WaitForChild("Script")
        local Event9 = Script13:WaitForChild("Event")
        Event9:FireServer(WaitForChild16, "Bottom", UDim2.new(0, 0, 0, 0), "both ðŸ¤", Color3.new(0.878431, 0.878431, 0.439216), "spray", "<font color=\\"#ff5555\\">N</font><font color=\\"#55ff55\\">o</font><font color=\\"#5599ff\\">Key</font>")
        local StarterGui6 = game:GetService("StarterGui")
        StarterGui6:SetCore("SendNotification", {
    Duration = 5,
    Text = "for debugging",
    Title = "RepStorage Brick Text Set"
})
        task.wait(0.15)
    end,
    Name = "set RepStorage brick's text"
})
local Button7 = Tab2:CreateButton({
    Callback = function(bI)
        local WaitForChild19 = game.ReplicatedStorage:WaitForChild("Brick")
        local Paint5 = object:FindFirstChild("Paint")
        local Script14 = Paint5:FindFirstChild("Script")
        local Event10 = Script14:FindFirstChild("Event")
    end,
    Name = "toxic repstorage part"
})
local Button8 = Tab2:CreateButton({
    Callback = function(bI)
        local WaitForChild20 = game.ReplicatedStorage:WaitForChild("Brick")
        local Paint6 = object:FindFirstChild("Paint")
        local Script15 = Paint6:FindFirstChild("Script")
        local Event11 = Script15:FindFirstChild("Event")
    end,
    Name = "unanchor repstorage part"
})
local Button9 = Tab2:CreateButton({
    Callback = function(bI)
        local WaitForChild21 = game.Players.LocalPlayer:WaitForChild("Backpack")
        local Delete3 = WaitForChild21:WaitForChild("Delete")
        local Script16 = Delete3:WaitForChild("Script")
        local Event12 = Script16:WaitForChild("Event")
        Event12:FireServer(game.ReplicatedStorage.Brick, game.ReplicatedStorage.Brick.Position)
    end,
    Name = "BREAK BKIT (EQUIP DELETE BEFORE RUN, UNEQUIP DELETE AFTER RUN)"
})
local Button10 = Tab2:CreateButton({
    Callback = function(bI)
        local GetDescendants = workspace.Bricks:GetDescendants()
    end,
    Name = "Delcubes all (no enlighten needed, just delete tool)"
})
local Button11 = Tab2:CreateButton({
    Callback = function(bI)
        local WaitForChild22 = game.ReplicatedStorage:WaitForChild("Brick")
        WaitForChild22.Parent = workspace
        print("setting material")
        local WaitForChild23 = game.Players.LocalPlayer.Character:WaitForChild("Paint")
        local Script17 = WaitForChild23:WaitForChild("Script")
        local Event13 = Script17:WaitForChild("Event")
        Event13:FireServer(WaitForChild22, "Left", UDim2.new(0, 0, 0, 0), "both ðŸ¤", Color3.new(0.878431, 0.878431, 0.439216), "plastic", "")
        print("setting spray")
        local WaitForChild24 = game.Players.LocalPlayer.Character:WaitForChild("Paint")
        local Script18 = WaitForChild24:WaitForChild("Script")
        local Event14 = Script18:WaitForChild("Event")
        Event14:FireServer(WaitForChild22, "Left", UDim2.new(0, 0, 0, 0), "both ðŸ¤", Color3.new(0.878431, 0.878431, 0.439216), "spray", "")
        WaitForChild22.Parent = game.ReplicatedStorage
        task.wait(0.2)
        warn("fixed side: Left", 1)
        local WaitForChild25 = game.ReplicatedStorage:WaitForChild("Brick")
        WaitForChild25.Parent = workspace
        print("setting material")
        local WaitForChild26 = game.Players.LocalPlayer.Character:WaitForChild("Paint")
        local Script19 = WaitForChild26:WaitForChild("Script")
        local Event15 = Script19:WaitForChild("Event")
        Event15:FireServer(WaitForChild25, "Right", UDim2.new(0, 0, 0, 0), "both ðŸ¤", Color3.new(0.878431, 0.878431, 0.439216), "plastic", "")
        print("setting spray")
        local WaitForChild27 = game.Players.LocalPlayer.Character:WaitForChild("Paint")
        local Script20 = WaitForChild27:WaitForChild("Script")
        local Event16 = Script20:WaitForChild("Event")
        Event16:FireServer(WaitForChild25, "Right", UDim2.new(0, 0, 0, 0), "both ðŸ¤", Color3.new(0.878431, 0.878431, 0.439216), "spray", "")
        WaitForChild25.Parent = game.ReplicatedStorage
        task.wait(0.2)
        warn("fixed side: Right", 2)
        local WaitForChild28 = game.ReplicatedStorage:WaitForChild("Brick")
        WaitForChild28.Parent = workspace
        print("setting material")
        local WaitForChild29 = game.Players.LocalPlayer.Character:WaitForChild("Paint")
        local Script21 = WaitForChild29:WaitForChild("Script")
        local Event17 = Script21:WaitForChild("Event")
        Event17:FireServer(WaitForChild28, "Back", UDim2.new(0, 0, 0, 0), "both ðŸ¤", Color3.new(0.878431, 0.878431, 0.439216), "plastic", "")
        print("setting spray")
        local WaitForChild30 = game.Players.LocalPlayer.Character:WaitForChild("Paint")
        local Script22 = WaitForChild30:WaitForChild("Script")
        local Event18 = Script22:WaitForChild("Event")
        Event18:FireServer(WaitForChild28, "Back", UDim2.new(0, 0, 0, 0), "both ðŸ¤", Color3.new(0.878431, 0.878431, 0.439216), "spray", "")
        WaitForChild28.Parent = game.ReplicatedStorage
        task.wait(0.2)
        warn("fixed side: Back", 3)
        local WaitForChild31 = game.ReplicatedStorage:WaitForChild("Brick")
        WaitForChild31.Parent = workspace
        print("setting material")
        local WaitForChild32 = game.Players.LocalPlayer.Character:WaitForChild("Paint")
        local Script23 = WaitForChild32:WaitForChild("Script")
        local Event19 = Script23:WaitForChild("Event")
        Event19:FireServer(WaitForChild31, "Front", UDim2.new(0, 0, 0, 0), "both ðŸ¤", Color3.new(0.878431, 0.878431, 0.439216), "plastic", "")
        print("setting spray")
        local WaitForChild33 = game.Players.LocalPlayer.Character:WaitForChild("Paint")
        local Script24 = WaitForChild33:WaitForChild("Script")
        local Event20 = Script24:WaitForChild("Event")
        Event20:FireServer(WaitForChild31, "Front", UDim2.new(0, 0, 0, 0), "both ðŸ¤", Color3.new(0.878431, 0.878431, 0.439216), "spray", "")
        WaitForChild31.Parent = game.ReplicatedStorage
        task.wait(0.2)
        warn("fixed side: Front", 4)
        local WaitForChild34 = game.ReplicatedStorage:WaitForChild("Brick")
        WaitForChild34.Parent = workspace
        print("setting material")
        local WaitForChild35 = game.Players.LocalPlayer.Character:WaitForChild("Paint")
        local Script25 = WaitForChild35:WaitForChild("Script")
        local Event21 = Script25:WaitForChild("Event")
        Event21:FireServer(WaitForChild34, "Top", UDim2.new(0, 0, 0, 0), "both ðŸ¤", Color3.new(0.878431, 0.878431, 0.439216), "plastic", "")
        print("setting spray")
        local WaitForChild36 = game.Players.LocalPlayer.Character:WaitForChild("Paint")
        local Script26 = WaitForChild36:WaitForChild("Script")
        local Event22 = Script26:WaitForChild("Event")
        Event22:FireServer(WaitForChild34, "Top", UDim2.new(0, 0, 0, 0), "both ðŸ¤", Color3.new(0.878431, 0.878431, 0.439216), "spray", "")
        WaitForChild34.Parent = game.ReplicatedStorage
        task.wait(0.2)
        warn("fixed side: Top", 5)
        local WaitForChild37 = game.ReplicatedStorage:WaitForChild("Brick")
        WaitForChild37.Parent = workspace
        print("setting material")
        local WaitForChild38 = game.Players.LocalPlayer.Character:WaitForChild("Paint")
        local Script27 = WaitForChild38:WaitForChild("Script")
        local Event23 = Script27:WaitForChild("Event")
        Event23:FireServer(WaitForChild37, "Bottom", UDim2.new(0, 0, 0, 0), "both ðŸ¤", Color3.new(0.878431, 0.878431, 0.439216), "plastic", "")
        print("setting spray")
        local WaitForChild39 = game.Players.LocalPlayer.Character:WaitForChild("Paint")
        local Script28 = WaitForChild39:WaitForChild("Script")
        local Event24 = Script28:WaitForChild("Event")
        Event24:FireServer(WaitForChild37, "Bottom", UDim2.new(0, 0, 0, 0), "both ðŸ¤", Color3.new(0.878431, 0.878431, 0.439216), "spray", "")
        WaitForChild37.Parent = game.ReplicatedStorage
        task.wait(0.2)
        warn("fixed side: Bottom", 6)
    end,
    Name = "remove bkit sprays (Broken)"
})
local Tab3 = Window:CreateTab("players", "users-round")
local Toggle9 = Tab3:CreateToggle({
    CurrentValue = false,
    Flag = "eue",
    Callback = function(enabled) end,
    Name = "enlightened users ESP"
})
local Toggle10 = Tab3:CreateToggle({
    CurrentValue = false,
    Flag = "dbp",
    Callback = function(enabled) end,
    Name = "invisible users ESP(best always on cuz enlightend esp doesnt show invisible enlightend users)"
})
task.spawn(function()
    task.wait(0.5)
    local GetPlayers = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers2 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers3 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers4 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers5 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers6 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers7 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers8 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers9 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers10 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers11 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers12 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers13 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers14 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers15 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers16 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers17 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers18 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers19 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers20 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers21 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers22 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers23 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers24 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers25 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers26 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers27 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers28 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers29 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers30 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers31 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers32 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers33 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers34 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers35 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers36 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers37 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers38 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers39 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers40 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers41 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers42 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers43 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers44 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers45 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers46 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers47 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers48 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers49 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers50 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers51 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers52 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers53 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers54 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers55 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers56 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers57 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers58 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers59 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers60 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers61 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers62 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers63 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers64 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers65 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers66 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers67 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers68 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers69 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers70 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers71 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers72 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers73 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers74 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers75 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers76 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers77 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers78 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers79 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers80 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers81 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers82 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers83 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers84 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers85 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers86 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers87 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers88 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers89 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers90 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers91 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers92 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers93 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers94 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers95 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers96 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers97 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers98 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers99 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers100 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers101 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers102 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers103 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers104 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers105 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers106 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers107 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers108 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers109 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers110 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers111 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers112 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers113 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlayers114 = game.Players:GetPlayers()
    task.wait(0.5)
    local GetPlay